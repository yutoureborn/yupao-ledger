# 芋炮小账本 v0.3.4 UI 重构 + 角色资产升级补丁

## 本次内容
- 桌面端主内容区与首页信息架构重做
- 12 列 Bento 响应式布局
- 修复超宽屏内容挤在右侧、左侧空白过大的问题
- 首页支出分类使用独立紧凑组件
- 全局导航、按钮、焦点、触控尺寸、响应式规则优化
- 连同 v0.3.3 正式 PNG / WEBP 角色资产一起打包
- 移除未使用的旧内联角色 SVG

## 覆盖方式
将补丁目录内部文件按原路径覆盖到当前 GitHub 仓库。

## 数据影响
- 无新 D1 migration
- 不修改用户、密码、账目、发票、预算数据
- 补丁不包含 `wrangler.jsonc`

## 验证
- `npm run build`
- `node --test tests/*.test.mjs`
- 当前结果：34/34 通过
