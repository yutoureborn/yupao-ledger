# UPGRADE v0.3.4

## 目标
彻底重做桌面首页的布局与交互层级，解决内容区过窄、右侧堆叠、超宽屏大面积空白的问题。

## 主要改动
1. 主内容区使用 `width: calc(100% - sidebar)`，最大宽度 1520px。
2. 首页改为 12 列 Bento 布局。
3. Hero、摘要、趋势、分类、明细、预算、发票重新编排。
4. 首页支出分类改为专用紧凑组件。
5. 保留并打包 v0.3.3 正式角色资产。
6. 移除未使用的旧内联角色 SVG。

## 数据影响
无数据库迁移，无 API 改动。
