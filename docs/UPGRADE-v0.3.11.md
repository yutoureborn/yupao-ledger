# v0.3.11 升级说明

## 本次更新
- 收支明细查询新增 **分类筛选**。
- PC：类型 / 分类 / 账户 / 商户备注搜索可组合筛选。
- 手机：在类型切换下方增加分类选择器。
- 切换收支类型时自动清空旧分类，避免支出分类残留到收入筛选。
- 转账没有分类，选择“转账”后分类筛选自动隐藏 / 禁用。

## API
后端已有 `/api/transactions?categoryId=...` 能力，本版本将其正式接入前端筛选。

## 数据影响
- 无 migration
- 无历史数据修改
